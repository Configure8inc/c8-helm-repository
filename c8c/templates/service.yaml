apiVersion: v1
kind: Service
metadata:
  name: {{ include "c8c.fullname" . }}
  labels:
    {{- include "c8c.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "c8c.selectorLabels" . | nindent 4 }}
